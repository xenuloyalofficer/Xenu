# 2026-02-18 — Wednesday

## Preferences
- Maria confirmed unit preference: metric only (kilograms and meters).
- Maria wants all repos, documentation, and heavy data kept on the external SSD mount (`/mnt/usb-ssd`) to preserve internal disk space.
- Maria wants a repeatable Windows↔Linux dev workflow using SSH local port forwarding so she can open Linux dev servers from Windows browser.

## Work Done
- Migrated key repo/documentation roots to external SSD and replaced legacy internal paths with symlinks:
  - `/home/mj/workspace` -> `/mnt/usb-ssd/repos/workspace`
  - `/home/mj/projects` -> `/mnt/usb-ssd/repos/projects`
  - `/home/mj/2` -> `/mnt/usb-ssd/repos/2`
  - `/home/mj/2ndbrain` -> `/mnt/usb-ssd/repos/2ndbrain`
  - `/home/mj/rune-memory` -> `/mnt/usb-ssd/repos/rune-memory`
- Removed temporary backup directories after validation.
- Updated path references in `MEMORY.md` and `TOOLS.md` to canonical SSD locations.
